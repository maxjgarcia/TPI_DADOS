realizar la funcion jugar []
realizar la funcion de tirada dados 12 [x]
mover menu a su propia funcion []
agregar pragma  once en los h []

notas:

el stock de dados hacerlos en un vector.

usar una flag "bool objetivoLogrado = false" que cambia a true en un if si se suma el num target para realizar la operacion de paso de dados

